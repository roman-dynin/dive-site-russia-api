name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

defaults:
  run:
    shell: bash
    working-directory: ./

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@master
      - name: Build
        run: ../dive-site-russia-deply/docker-compose up -d --build --no-deps api
      - name: Migrations
        run: ../dive-site-russia-deply/docker-compose exec api php artisan migrate
